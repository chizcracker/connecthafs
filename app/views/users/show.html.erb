<!DOCTYPE html>
<html>
<body>
  <%= react_component(
    'UserProfile',
    id: @user.id,
    name: @user.name,
    phonenumber: @user.phonenumber,
    classyear: @user.class_year,
    address: @user.address
  ) %>
</body>
</html>
